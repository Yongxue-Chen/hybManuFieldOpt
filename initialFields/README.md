# initialFields

This directory is reserved for preprocessed initial field data expected by the training scripts.

The unified location for externally generated initial fields is `model_data/initial_fields/<model>/`.
