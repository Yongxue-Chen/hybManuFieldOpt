# initialFields

This directory is reserved for preprocessed initial field data expected by the training scripts.

The public source release does not include the full generated dataset. For small runnable examples, place curated demo files under `demo_data/` and copy or symlink them here when needed.
