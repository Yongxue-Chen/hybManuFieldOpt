# demo_data

Place small public demo assets here, such as lightweight STL files, tiny initial field samples, and small initial model weights used by README examples.

Keep this directory small enough for source distribution. Large datasets and trained checkpoints should be hosted externally.
